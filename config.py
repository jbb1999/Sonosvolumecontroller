max_volume = 25 #sets the maximum the volume can go before the bot starts lowering it.
volume_damping_speed = 10 #how fast the volume should dampen when it detects it above max volume
check_interval = 0.5 #how often the bot checks the volume.
volume_lowering_goal = 20 # what the volume should become when triggered.
volume_lowering_mode = 2 #1 means it instantly gets set to desired volume, 2 means get sets to 0 then increses.
